[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
winit = "0.30.10"
anyhow = "1.0.100"
gl = { path = "../gl" }

[dev-dependencies]
gl_headless = { path = "../gl-headless" }
